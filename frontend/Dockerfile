# NOTE: REACT_APP_BACKEND_URL is baked into the JS bundle at BUILD time (CRA
# convention), not read at container start. Pass it as a build arg pointing
# at your backend service's public URL, and rebuild whenever that URL changes.
FROM node:20-alpine AS build
WORKDIR /app
COPY package.json yarn.lock ./
RUN yarn install --frozen-lockfile
COPY . .
ARG REACT_APP_BACKEND_URL
ENV REACT_APP_BACKEND_URL=$REACT_APP_BACKEND_URL
RUN CI=true yarn build

FROM nginx:alpine
COPY --from=build /app/build /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
